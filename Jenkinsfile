pipeline {
    agent {
        label 'jenkinsslave'
    }
    environment {
        name = "sudha"
        platform = "aws"
    }
    stages {
        stage (fisrst stage) {
            steps {
                echo "welcome ${name}"
                echo "you are enrolled to ${platform}"
            }
        }
    }
}
