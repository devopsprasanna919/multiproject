pipeline {
    agent {
        label 'jenkinsslave'
    }
    environment {
        name = sudha
        platform = devops
    }
    stages {
        stage (fisrst stage) {
            steps {
                echo "welcome ${name}"
                echo "joined ${platform}"
            }
        }
    }
}
