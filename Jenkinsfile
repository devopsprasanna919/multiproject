pipeline {
    agent {
        label "jenkinsslave"
    }
    stages {
        stage ('build') {
           environment {
            course = "aws"
           }
            steps {
                echo ("registered for course ${aws}")
            }
        }
    }
}
// env pipeline
